[package]
name = "pkinit-trust-proto"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Varlink interface for the PKINIT KDC-CA trust broker"

[dependencies]
zlink = { version = "0.7", features = ["smol"] }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
